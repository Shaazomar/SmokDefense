import { StubPage } from "@/components/sections/StubPage";

export default function TechnologyPage() {
  return (
    <StubPage
      eyebrow="Technology"
      title="Control. Network. Edge."
      description="Field devices, local control equipment, secure connectivity, and platform intelligence, engineered as one system."
    />
  );
}
