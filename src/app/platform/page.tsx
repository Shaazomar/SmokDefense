import { StubPage } from "@/components/sections/StubPage";

export default function PlatformPage() {
  return (
    <StubPage
      eyebrow="Platform"
      title="The Digital Layer"
      description="Monitoring, events, diagnostics, maintenance, and analytics for complex building life-safety systems — built on engineered local control that stays independent of cloud availability."
    />
  );
}
